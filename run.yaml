apiVersion: tekton.dev/v1beta1
kind: PipelineRun
metadata:
  name: tax-pipeline-run
spec:
  pipelineRef:
    name: tax-pipeline

  params:
    - name: IMAGE
      value: us.icr.io/namespace/tax-calculator

  workspaces:
    - name: shared-workspace
      emptyDir: {}
